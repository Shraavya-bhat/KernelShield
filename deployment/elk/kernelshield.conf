input {
  file {
    path => "/home/madhura/Desktop/KernelShield/logs/security_logs.json"
    start_position => "beginning"
    sincedb_path => "/dev/null"
    codec => json
  }
}

output {
  elasticsearch {
    hosts => ["https://localhost:9200"]
    user => "elastic"
    password => "QX45wi_VdQeu6R-Zl3cL"
    ssl_enabled => true
    ssl_verification_mode => "none"
    index => "kernelshield-logs"
  }

  stdout {
    codec => rubydebug
  }
}
